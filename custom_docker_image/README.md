pip freeze -- to know about the packages.. which packages are installed.
pip freeze > requirement.txt--- to create file of installed packages

To create a virtual env--
python -m venv venv
To activate it--- venv\Scripts\activate
